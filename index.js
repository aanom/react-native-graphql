/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import Customers from './src/customers'
AppRegistry.registerComponent(appName, () => Customers);
